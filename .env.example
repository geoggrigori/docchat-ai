# Copy this file to .env.local and fill in your key.
#
# Without a key the app still runs in "demo mode": retrieval works and the UI
# shows the matched passages. With a key, Claude generates real cited answers.
#
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
